name: Build & Push notification-service

on:
  push:
    paths: ['notification-service/**', '!notification-service/helm/**']
    branches: [ main ]
  pull_request:
    paths: ['notification-service/**', '!notification-service/helm/**']
    branches: [ main ]
  workflow_dispatch:

jobs:
  call-reusable:
    uses: ./.github/workflows/_ci-service.yml
    with:
      service_name: notification-service
    secrets: inherit
